#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t,b;
    cin>>t>>b;
    while(t--)
    {
        string str;
        for(int i=0;i<=9;i++)
        {
            char ch;
            cout<<(i+1)<<"\n";
            cout.flush();
            cin>>ch;
            str=str+ch;
        }
        cout<<str<<"\n";
        cout.flush();
        char x;
        cin>>x;
        if(x=='N' || x=='n')
        {
            break;
        }
    }
    return(0);
}
